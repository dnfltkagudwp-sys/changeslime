using UnityEngine;

/// <summary>
/// 독성 안개(ToxicMist) 오브젝트임을 표시하는 빈 마커. 위험 판정(SpikeTrap)에는 관여하지 않고,
/// GameHUD가 "독성 안개가 앞에 있는지"를 감지해 사전 경고를 띄우는 용도로만 사용함.
/// </summary>
public class ToxicMistMarker : MonoBehaviour
{
}
